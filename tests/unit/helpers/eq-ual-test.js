import { eqUal } from '../../../helpers/eq-ual';
import { module, test } from 'qunit';

module('Unit | Helper | eq ual');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = eqUal(42);
  assert.ok(result);
});
